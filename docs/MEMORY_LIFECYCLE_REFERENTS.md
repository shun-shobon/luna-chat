# 記憶ライフサイクルの対応表

| 出典       | 目的                              | 具体対象                                                                             | 役割     | 前後関係                                    | 候補語          | 初出定義                                                                                                           |
| ---------- | --------------------------------- | ------------------------------------------------------------------------------------ | -------- | ------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------ |
| 利用者要件 | idle終了時に会話内容を残す        | conversation threadをarchiveする直前に、同じthreadへ日次fileの更新を依頼する追加turn | 手段     | idle判定 → 追加turn → archive               | session記憶保存 | session記憶保存とは、idle終了するconversation thread自身が日次fileへ会話要約と将来役立つ事項を追記する処理を指す。 |
| 利用者要件 | 日ごとの会話記録を保持する        | 保存開始日の`memory/YYYY-MM-DD.md`へsession単位で追記されるMarkdown                  | 記録     | session記憶保存 → 日次整理                  | 日次記憶file    | 日次記憶fileとは、同じ暦日に終了したsessionの記憶を保持する`memory/YYYY-MM-DD.md`を指す。                          |
| 利用者回答 | idle終了以外では追加保存しない    | shutdown・通常turn失敗・connection loss・fatal abortによるthread終了                 | 開始条件 | 終了理由判定 → 直接archive                  | idle終了        | 既存のsession idle timerが満了し、通常の会話処理が完了した状態を指す。                                             |
| 利用者回答 | 保存中の新着を旧sessionへ混ぜない | session記憶保存中に受理したDiscord投稿を次のthread用queueへ積む処理                  | 手段     | 追加turn実行中 → queue → archive → 新thread | 次thread用queue | 既存conversation scopeに届いた新着投稿を、旧threadのarchive後に新threadで処理する待ち行列を指す。                  |

| 出典       | 目的                              | 具体対象                                                                                                 | 役割 | 前後関係                                             | 候補語               | 初出定義                                                                                        |
| ---------- | --------------------------------- | -------------------------------------------------------------------------------------------------------- | ---- | ---------------------------------------------------- | -------------------- | ----------------------------------------------------------------------------------------------- |
| 利用者要件 | 長期記憶とworkspaceを毎日整理する | process timezoneの設定cronにより新規専用threadを開き、全日次記憶file・MEMORY.md・workspaceを整理する処理 | 手段 | 定刻到達 → 専用thread → file操作 → Git保存 → archive | 日次整理             | 日次整理とは、組み込みscheduleが専用threadへ記憶とworkspaceの整理を依頼する処理を指す。         |
| 利用者回答 | 実行条件を明示する                | startup時に読むmemory設定のenabledと5-field cron                                                         | 値   | config読込 → controller登録                          | memory設定           | memory設定とは、session記憶保存と日次整理を一括で有効化し、日次整理のcronを指定する設定を指す。 |
| 利用者回答 | agentの裁量でworkspaceを整理する  | 通常threadと同じ権限で行うfile削除・移動・rename・文書整理                                               | 手段 | 専用thread開始 → workspace調査 → file操作            | workspace整理        | workspace整理とは、専用threadが通常threadと同じ権限で不要fileの削除や配置変更を行う処理を指す。 |
| 利用者回答 | 日次記憶の原記録を保持する        | 整理後も同じpathに残す全`memory/YYYY-MM-DD.md`                                                           | 記録 | 日次整理前 → 内容整理 → 保持                         | 保持中の日次記憶file | 保持中の日次記憶fileとは、日次整理後も削除・移動せず`memory/`に残す日次記憶fileを指す。         |
| 利用者回答 | 停止中の定刻を後から実行しない    | 04:00にprocessが停止していた場合の未実行分                                                               | 状態 | 停止中の定刻 → 実行なし → 次回定刻                   | 未実行日             | 未実行日とは、process停止中にcron時刻を過ぎ、日次整理が起動しなかった日を指す。                 |

| 出典       | 目的                                  | 具体対象                                                                         | 役割 | 前後関係                                | 候補語     | 初出定義                                                                                                          |
| ---------- | ------------------------------------- | -------------------------------------------------------------------------------- | ---- | --------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------- |
| 利用者回答 | workspaceの変更をlocal履歴へ保存する  | 日次整理threadが整理後にworkspaceの差分を選別して作る1件のcommit                 | 記録 | workspace整理 → 差分確認 → commit       | 整理commit | 整理commitとは、日次整理threadがworkspace整理後に必要と判断した差分をlocal Git repositoryへ保存するcommitを指す。 |
| 利用者回答 | repository未作成時にもGit保存を試みる | Gitが利用可能なら専用threadが行うrepository初期化とlocal identity設定            | 手段 | Git確認 → repository初期化 → 整理commit | Git初期化  | Git初期化とは、日次整理threadがworkspaceをrepository化し、`Luna <luna@localhost>`をlocal設定する処理を指す。      |
| 利用者回答 | Gitがない環境でもfile整理を行う       | Git executableが見つからない場合に整理commitだけを省略して日次整理を継続する挙動 | 手段 | Git確認失敗 → file整理 → archive        | Git省略    | Git省略とは、Git executableが利用できない場合に限り、整理commitを作らず日次整理を継続する明示的fallbackを指す。   |
