# turn通知所有権の対応表

| 出典          | 目的                                                           | 具体対象                                                                                           | 役割 | 前後関係                                                 | 候補語               | 初出定義                                                                                                      |
| ------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ---- | -------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------- |
| 添付ログ      | 正常な親turnの継続中にconnection全体が破損扱いになる原因を示す | Lunaが開始した親threadとは異なるthread IDを持ち、同じapp-server接続へ届いた`turn/started`          | 事象 | 親`turn/start`応答 → 別threadの通知 → connection failure | 管理外thread通知     | 管理外thread通知とは、Lunaが開始して現在所有するthread集合に属さないthread IDを持つ通知を指す。               |
| 現行source    | Lunaが相関を保証できる範囲を固定する                           | `startTurn`を実行してからarchive処理を終えるまでLunaが所有するthread IDの集合                      | 状態 | thread開始 → turn相関 → archive                          | 管理中thread         | 管理中threadとは、Lunaがturnを実行するため所有し、archive完了まで追跡するCodex threadを指す。                 |
| Codex 0.145.0 | 親turnとsubagent turnを同じ接続上で安全に分離する              | 管理外thread通知はLunaのturn trackerへ渡さず、管理中threadの通知だけをactive trackerへ相関する処理 | 手段 | 通知受信 → thread ID検証 → 所有判定 → tracker相関        | thread所有境界       | thread所有境界とは、通知のthread IDが管理中thread集合に含まれるかでLunaの相関責任を分ける境界を指す。         |
| 既存障害契約  | 管理中thread内の順序・ID破損を検出し続ける                     | 管理中threadにactive trackerがない通知、またはactive turnと異なるturn IDを持つ通知                 | 事象 | 所有判定 → 相関失敗 → protocol fatal                     | 管理中thread相関違反 | 管理中thread相関違反とは、Lunaが所有するthread内で通知を現在のactive turnへ一意に対応づけられない状態を指す。 |
