services:
  luna-chat:
    image: ghcr.io/shun-shobon/luna-chat:latest
    env_file:
      - .env
    environment:
      TZ: ${TZ:-Asia/Tokyo}
    volumes:
      - ./data:/home/node
    init: true
    restart: unless-stopped
